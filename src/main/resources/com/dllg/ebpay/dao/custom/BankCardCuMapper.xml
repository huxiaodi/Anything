<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dllg.ebpay.dao.custom.BankCardCuMapper">
    
    <select id="getBindCardBySubNo" resultType="com.dllg.ebpay.model.response.WithdrawalBankCardResponse">
        SELECT
            bank_card_id,
            bank_card_no,
            bank_card_bank
        FROM bank_card
        WHERE bank_card_status = 2
        AND bank_card_account_sub_id = (
            SELECT account_sub_id
            FROM  account_sub
            WHERE account_sub_no = #{accountNo}
        )
    </select>
    
</mapper>