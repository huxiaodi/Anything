<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dllg.ebpay.dao.custom.InterestDictCuMapper">

    <!--列表 -->
    <select id="dataList" resultType="map">
        SELECT
        interest_id interestId,
        interest_date interestDate,
        interest_value interestValue,
        interest_file interestFile,
        interest_file_url interestFileUrl,
        create_user_id createUserId,
        create_time createTime,
        update_user_id updateUserId,
        update_time updateTime
        FROM
        dict_interest
        <where>
            <if test="startTime!=null">
                and create_time <![CDATA[ >= ]]> #{startTime}
            </if>
            <if test="endTime!=null">
                and create_time <![CDATA[ <= ]]> DATE_ADD(#{endTime},INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY interest_date
    </select>

    <select id="selectInterest" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT interest_value FROM dict_interest
        WHERE interest_date = #{interestDate}
    </select>


    <insert id="add" parameterType="com.dllg.ebpay.model.generator.InterestDict">

        insert into dict_interest (
        interest_id,
        interest_date,
        interest_value,
        interest_file ,
        interest_file_url ,
        create_user_id ,
        create_time
       )
        values (
        #{interestId},
        #{interestDate},
        #{interestValue},
        #{interestFile},
        #{interestFileUrl},
        #{createUserId},
        #{createTime}
       )
    </insert>

    <update id="update" parameterType="com.dllg.ebpay.model.generator.InterestDict">
        update dict_interest
        set
        interest_value = #{interestValue},
        interest_file = #{interestFile},
        interest_file_url = #{interestFileUrl},
        update_user_id = #{updateUserId},
        update_time = #{updateTime}
        where interest_id = #{interestId};
    </update>


    <delete id="deletes" parameterType="java.util.List">
        delete from dict_interest where interest_id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>



    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultType="map">
        select
        <include refid="Base_Column_List" />
        from dict_interest
        where interest_date = #{interestDate}
    </select>

    <sql id="Base_Column_List">
        interest_date , interest_value , interest_file ,
        create_user_id , create_time , update_user_id , update_time updateTime
    </sql>

    <select id="selectByExample"  resultType="map">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from dict_interest
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>

            order by interest_date

    </select>

    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

</mapper>