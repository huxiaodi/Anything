<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.manage.dao.custom.KindDictCuMapper">

    <!--列表 -->
    <select id="dataList" resultType="map">
        SELECT
        kind_id kindId,
        kind_code kindCode,
        kind_name kindName,
        create_user_id createUserId,
        create_time createTime,
        update_user_id updateUserId,
        update_time updateTime
        FROM
        dict_kind
        <where>
            <if test="startTime!=null">
                and create_time <![CDATA[ >= ]]> #{startTime}
            </if>
            <if test="endTime!=null">
                and create_time <![CDATA[ <= ]]> DATE_ADD(#{endTime},INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY kind_code
    </select>


    <insert id="add" parameterType="com.test.manage.model.generator.KindDict">

        insert into dict_kind (
        kind_id,
        kind_code,
        kind_name,
        create_user_id ,
        create_time
       )
        values (
        #{kindId},
        #{kindCode},
        #{kindName},
        #{createUserId},
        #{createTime}
       )
    </insert>

    <update id="update" parameterType="com.test.manage.model.generator.KindDict">
        update dict_kind
        set
        kind_name = #{kindName},
        update_user_id = #{updateUserId},
        update_time = #{updateTime}
        where kind_id = #{kindId};
    </update>


    <delete id="deletes" parameterType="java.util.List">
        delete from dict_kind where kind_id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>



    <select id="selectByExample"  resultType="map">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from dict_kind
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultType="map">
        select
        <include refid="Base_Column_List" />
        from dict_kind
        where kind_name = #{kindName}
    </select>

    <sql id="Base_Column_List">
        kind_code , kind_name ,
        create_user_id , create_time , update_user_id , update_time updateTime
    </sql>

    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

</mapper>