<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.manage.dao.custom.CustomerDictCuMapper">

    <!--列表 -->
    <select id="dataList" resultType="map">
        SELECT
        customer_id customerId,
        customer_code customerCode,
        customer_name customerName,
        create_user_id createUserId,
        create_time createTime,
        update_user_id updateUserId,
        update_time updateTime
        FROM
        dict_customer
        <where>
            <if test="startTime!=null">
                and create_time <![CDATA[ >= ]]> #{startTime}
            </if>
            <if test="endTime!=null">
                and create_time <![CDATA[ <= ]]> DATE_ADD(#{endTime},INTERVAL 1 DAY)
            </if>
        </where>
        ORDER BY customer_code
    </select>


    <insert id="add" parameterType="com.test.manage.model.generator.CustomerDict">

        insert into dict_customer (
        customer_id,
        customer_code,
        customer_name,
        create_user_id ,
        create_time
       )
        values (
        #{customerId},
        #{customerCode},
        #{customerName},
        #{createUserId},
        #{createTime}
       )
    </insert>

    <update id="update" parameterType="com.test.manage.model.generator.CustomerDict">
        update dict_customer
        set
        customer_name = #{customerName},
        update_user_id = #{updateUserId},
        update_time = #{updateTime}
        where customer_id = #{customerId};
    </update>


    <delete id="deletes" parameterType="java.util.List">
        delete from dict_customer where customer_id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>



    <select id="selectByExample"  resultType="map">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List" />
        from dict_customer
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>



    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultType="map">
        select
        <include refid="Base_Column_List" />
        from dict_customer
        where customer_code = #{customerCode}
    </select>

    <sql id="Base_Column_List">
        customer_code , customer_name ,
        create_user_id , create_time , update_user_id , update_time updateTime
    </sql>

    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

</mapper>