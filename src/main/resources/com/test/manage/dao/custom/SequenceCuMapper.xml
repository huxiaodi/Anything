<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.manage.dao.custom.SequenceCuMapper">
    <update id="next" >
        UPDATE sequence SET sequence_id=LAST_INSERT_ID(sequence_id+1) where sequence_name=#{sequenceName}
        <selectKey keyProperty="sequenceId" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </update>
    <select id="now" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT sequence_id FROM sequence where sequence_name=#{sequenceName}
    </select>
</mapper>